Options -Indexes

<FilesMatch "^\.|(\.env|composer\.(json|lock)|.*\.(sql|log|bak|ini|md))$">
    Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(app|config|database|scripts|vendor|\.git|\.env)(/|$) - [F,L]
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
